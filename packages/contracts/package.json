{
  "name": "@agent-core/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Tipos canónicos + interfaces de providers + tipo Manifest. Sin lógica; lo importan todos.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  }
}
