{
  "name": "@agent-core/agents",
  "version": "0.0.0",
  "private": true,
  "description": "Catálogo de agentes reutilizables. Usan solo contracts + core; no conocen el dominio.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@agent-core/contracts": "*"
  }
}
